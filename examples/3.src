(def (map f l)
  (case l
    (Nil Nil)
    ((Cons h t) (Cons (f h) t))))

(def (head l)
  (case l
    (Nil 0)
    ((Cons h t) h)))

(def (from n) (Cons n (from (+ 1 n))))

(def (main n) 
  (Cons (head (map (+ 2) (from 1))) Nil))


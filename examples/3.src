map f l =
  case l of
  { Nil -> Nil
  ; Cons h t -> Cons (f h) (map f t)
  };

head l =
  case l of
  { Nil -> 0
  ; Cons h t -> h
  };

from n = Cons n (from (add 1 n));

-- [3]
start n = Cons (head (map (add 2) (from 1))) Nil
ifte b x y = case b of
  { True  -> x
  ; False -> y
  };

from n = Cons n (from (add 1 n));

notDivide p n = ne 0 (rem n p);

id x = x;

filter f l = case l of
  { Nil -> Nil
  ; Cons h t -> (ifte (f h) (Cons h) id) (filter f t)
  };

sieve l = case l of
  { Nil -> Nil
  ; Cons p rest -> Cons p (sieve (filter (notDivide p) rest))
  };

primes = sieve (from 2);

take n l = case l of
  { Nil -> Nil
  ; Cons h t -> ifte (eq 0 n) Nil (Cons h (take (sub n 1) t))
  };

start n = take n primes